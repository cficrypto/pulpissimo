#
# List of IPs and relative branch/commit-hash/tag.
# Uses the YAML syntax.
#
# Examples:
#
#   or10n:
#     commit: tags/PULP3_final
#     domain: [cluster]
#   udma:
#     commit: 62b10440
#     domain: [soc]
#   axi_slice:
#     commit: master
#     domain: [soc,cluster]
# If a *tag* or *commit* is referenced, the IP will be in a
#  state of DETACHED HEAD. Before committing any additional
#  work, make sure to checkout a branch.
#

pulp_soc:
#  commit: master_v6.0
#  server: https://github.com
#  group:  stefanct
  server: git@gitlab.ecs.tuwien.ac.at
# group:  alwieser
  group:  riscv_cfi_crypto
  commit: 7883b7b2c243d53c93e6dd2d7ece52a4efe9ee40
  access_token: glpat-spzQ4xLxTAutiELem5CR

tbtools:
  commit: v0.1
  server: https://github.com
  group:  pulp-platform
